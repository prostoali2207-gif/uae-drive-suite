name: Apply vehicle timeline edit

on:
  push:
    branches:
      - feature/edit-vehicle-timeline
    paths:
      - .github/workflows/apply-vehicle-timeline-edit.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/edit-vehicle-timeline
          fetch-depth: 0

      - name: Patch VehicleTimelineSheet
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path('src/components/VehicleTimelineSheet.tsx')
          source = path.read_text()

          import_anchor = 'import { SupabaseClient } from "@supabase/supabase-js";\n'
          import_line = 'import { VehicleTimelineEditPanel } from "@/components/VehicleTimelineEditPanel";\n'
          if import_line not in source:
              if import_anchor not in source:
                  raise SystemExit('SupabaseClient import anchor not found')
              source = source.replace(import_anchor, import_anchor + import_line, 1)

          render_anchor = '''                                </dl>
                              </AccordionContent>'''
          render_block = '''                                </dl>
                                <VehicleTimelineEditPanel
                                  node={node}
                                  contractId={contractId}
                                  isFirstVehicle={history[0]?.id === node.id}
                                  isActive={active}
                                  onSaved={fetchHistory}
                                />
                              </AccordionContent>'''
          if '<VehicleTimelineEditPanel' not in source:
              if render_anchor not in source:
                  raise SystemExit('Timeline details render anchor not found')
              source = source.replace(render_anchor, render_block, 1)

          path.write_text(source)
          PY

      - name: Remove one-time workflow
        run: rm .github/workflows/apply-vehicle-timeline-edit.yml

      - name: Commit patch
        run: |
          git config user.name "fleetdesk-bot"
          git config user.email "actions@users.noreply.github.com"
          git add src/components/VehicleTimelineSheet.tsx src/components/VehicleTimelineEditPanel.tsx .github/workflows/apply-vehicle-timeline-edit.yml
          git commit -m "Add inline vehicle history correction"
          git push origin feature/edit-vehicle-timeline
